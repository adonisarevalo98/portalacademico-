import { BuscadorListFederadoPipe } from './buscador-list-federado.pipe';

describe('BuscadorListFederadoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorListFederadoPipe();
    expect(pipe).toBeTruthy();
  });
});
