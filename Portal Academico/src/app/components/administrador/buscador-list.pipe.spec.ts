import { BuscadorListPipe } from './buscador-list.pipe';

describe('BuscadorListPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorListPipe();
    expect(pipe).toBeTruthy();
  });
});
