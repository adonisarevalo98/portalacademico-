import { BuscadorListCursosPipe } from './buscador-list-cursos.pipe';

describe('BuscadorListCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorListCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
