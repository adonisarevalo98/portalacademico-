import { BuscadorListCursosbPipe } from './buscador-list-cursosb.pipe';

describe('BuscadorListCursosbPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorListCursosbPipe();
    expect(pipe).toBeTruthy();
  });
});
